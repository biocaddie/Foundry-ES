package org.neuinfo.foundry.common.ingestion;

/**
 * Created by bozyurt on 2/22/17.
 */
public enum TokenType {
    DOWNLOAD, EXTRACT, PARTITION, JOIN, INGEST, AS, BY, FROM, PER,
    WITH, FORMAT, START, SEMICOLON, COMMENT_START, COMMENT_END, STRING, LITERAL,
    FOR, SET, EQUAL, COMMA, USER, PWD
}
